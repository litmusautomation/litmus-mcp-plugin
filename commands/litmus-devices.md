---
description: List all devices on the active Litmus Edge instance as a table.
---

Call `dh_list_devices`. Present the result as a markdown table with columns: ID, Name, Driver, Status. No prose around it.
