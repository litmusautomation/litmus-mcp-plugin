---
description: List all devices on the active Litmus Edge instance as a table.
---

Call `get_devicehub_devices`. Present the result as a markdown table with columns: ID, Name, Driver, Status. No prose around it.
